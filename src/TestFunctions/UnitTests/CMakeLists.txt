if(UNIT_TESTS)
	set(src_files standard_test_functions_test.cpp test_functions_test.cpp)
	add_executable(runTestsTestFunctions runTests.cpp ${src_files})
	find_package(Armadillo REQUIRED)
	include_directories(${ARMADILLO_INCLUDE_DIRS})
	target_link_libraries(runTestsTestFunctions gtest_main)
	target_link_libraries(runTestsTestFunctions ${ARMADILLO_LIBRARIES})
	target_link_libraries(runTestsTestFunctions   testFunctions)
	target_link_libraries(runTestsTestFunctions   bounds)
	target_link_libraries(runTestsTestFunctions   optimizers)
	target_link_libraries(runTestsTestFunctions   lhs)
	target_link_libraries(runTestsTestFunctions   auxiliary)
	
	find_package(OpenMP)
	if(OpenMP_CXX_FOUND)	
		target_link_libraries(runTestsTestFunctions OpenMP::OpenMP_CXX)
	endif()

endif(UNIT_TESTS)
